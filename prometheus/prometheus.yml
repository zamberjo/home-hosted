global:
  scrape_interval: 120s
  evaluation_interval: 120s
  external_labels:
    monitor: 'rpi3b-stack-monitor'

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

# rule_files:
#   - "rules.yml"
